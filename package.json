{
  "name": "lexin-chrome-extension",
  "version": "1.8.0",
  "description": "Swedish to other languages dictionary. Powered by Lexin and Folkets Lexikon",
  "scripts": {
    "clean": "rm -rf dist",
    "prebuild": "npm run clean",
    "build:ts": "tsc",
    "build:bundle": "node build.js",
    "build:copy": "cp -r src/css dist/ && cp -r src/html dist/ && cp -r src/icons dist/ && cp src/manifest.json dist/",
    "build": "npm run build:ts && npm run build:bundle && npm run build:copy",
    "watch": "tsc --watch",
    "lint": "eslint 'src/**/*.{ts,js}'",
    "lint:fix": "eslint 'src/**/*.{ts,js}' --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "package": "npm run build && cd dist && zip -r ../lexin-extension.zip .",
    "dev": "npm run build && npm run watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/jquery": "^3.5.33",
    "@typescript-eslint/eslint-plugin": "^7.16.0",
    "@typescript-eslint/parser": "^7.16.0",
    "@vitest/ui": "^3.2.4",
    "esbuild": "^0.23.0",
    "eslint": "^8.57.0",
    "jquery": "^3.7.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3",
    "vitest": "^3.2.4"
  },
  "type": "module"
}
